version: "3.9"

services:
  web:
    image: ghcr.io/mazyod/phoenixsharp/server:latest
    build: .
    ports:
      - 4000:4000
    environment:
      - SECRET_KEY_BASE=integration-tester-not-secret-at-least-64-bytes-long-placeholder-value
      - PHX_HOST=localhost
